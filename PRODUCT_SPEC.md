# AI ALPHA PULSE — Спецификация продукта
## Полное техническое и продуктовое описание

---

## 1. Общая концепция

### 1.1 Что такое AI Alpha Pulse
AI Alpha Pulse — это интеллектуальная платформа мультифакторного скоринга финансовых активов (акций и криптовалют), которая использует 7 независимых AI-агентов для анализа рынка в реальном времени и выдачи торговых сигналов.

### 1.2 Миссия продукта
Дать розничным инвесторам и трейдерам доступ к институциональному уровню анализа без необходимости в дорогих терминалах (Bloomberg, Refinitiv) или глубоких знаниях технического анализа.

### 1.3 Целевая аудитория
- Розничные инвесторы с базовым опытом (1-3 года)
- Трейдеры среднего уровня ищущие подтверждение сигналов
- Финансовые блогеры и аналитики нуждающиеся в быстрой оценке активов
- Студенты финансовых специальностей

---

## 2. Ключевая ценность (Value Proposition)

### 2.1 Уникальное торговое предложение (UTP)
**"7 агентов — 1 решение. AI-скоринг активов за 2 секунды вместо 2 часов ручного анализа"**

### 2.2 Проблемы, которые решает продукт
| Проблема пользователя | Как решает AI Alpha Pulse |
|-----------------------|---------------------------|
| Информационная перегрузка (100+ индикаторов) | Сводит к 1 числу (-100 до +100) + сигнал |
| Необходимость анализа множества источников | Агрегация 7 факторов в реальном времени |
| Эмоциональные решения | Объективный AI-скоринг без FOMO/страха |
| Дорогие профессиональные терминалы | Бесплатные источники данных + открытая архитектура |
| Отсутствие времени на анализ | Автообновление каждые 15 минут |

### 2.3 Конкурентное преимущество
- **7 факторов vs 2-3** у конкурентов (TradingView, Simply Wall St)
- **Мульти-ассет**: акции США + Россия + крипто в одном интерфейсе
- **Explainable AI**: каждый скор объясняется (не чёрный ящик)
- **Открытый код**: возможность самостоятельной доработки

---

## 3. Архитектура скоринга (Scoring Engine)

### 3.1 Финальный AI SCORE
**Диапазон:** -100 (STRONG SELL) → 0 (NEUTRAL) → +100 (STRONG BUY)

**Формула:**
```
AI_SCORE = Σ(factor_score × weight)
```

### 3.2 Веса факторов (статические v0.3)

| Фактор | Вес | Обоснование |
|--------|-----|-------------|
| Trend | 35% | Тренд — главный драйвер цены |
| Sentiment | 20% | Психология рынка критична для тайминга |
| Fundamental | 20% | Долгосрочная основа актива |
| Relative Strength | 10% | Сравнение с рынком важно |
| Volatility | 5% | Управление рисками |
| Insider/Funds | 5% | Smart money индикатор |
| Macro | 5% | Рыночный контекст |

**Примечание:** Веса настраиваемые через конфиг. Возможность ML-оптимизации под разные режимы рынка (бычий/медвежий/боковик).

### 3.3 Детальное описание каждого фактора

#### 3.3.1 TREND AGENT (Тренд)
**Вес: 35%**

**Индикаторы:**
- MA20/50 crossover (Golden Cross / Death Cross)
- RSI(14) — перекупленность/перепроданность
- 5-day momentum (rate of change)

**Методология:**
```python
# Z-score нормализация каждого индикатора
# Композитный score от -100 до +100
# +100 = все сигналы на покупку
# -100 = все сигналы на продажу
```

**Источники данных:** OHLCV от Yahoo Finance / Binance / MOEX

---

#### 3.3.2 SENTIMENT AGENT (Настроение)
**Вес: 20%**

**Индикаторы:**
- CCI (Commodity Channel Index)
- Williams %R
- VPT (Volume Price Trend)
- Alpha Vantage News API — сентимент заголовков новостей

**Методология:**
- Технический сентимент: Z-score осцилляторов
- Новостной сентимент: NLP-анализ заголовков (при наличии API ключа)

**Источники данных:** OHLCV + Alpha Vantage News API

---

#### 3.3.3 FUNDAMENTAL AGENT (Фундаментал)
**Вес: 20%**

**Метрики для акций:**
- P/E (Price-to-Earnings) — чем ниже, тем лучше (с отраслевой нормализацией)
- P/B (Price-to-Book)
- ROE (Return on Equity) — рентабельность капитала
- Revenue Growth (QoQ, YoY)
- Debt-to-Equity
- Operating Margin / Net Margin

**Методология:**
```python
# Нормализация относительно сектора
# Z-score каждой метрики
# Композитный score
```

**Источники данных:** Yahoo Finance (yfinance library)

**Ограничение:** Для криптовалют фундаментал ограничен (используются on-chain метрики при доступности)

---

#### 3.3.4 RELATIVE STRENGTH AGENT (Относительная сила)
**Вес: 10%**

**Методология:**
- Сравнение актива с бенчмарком (S&P 500 для акций, BTC для крипто)
- Расчёт относительной доходности за периоды: 5, 10, 20, 60 дней
- Z-score отклонений

**Интерпретация:**
- Положительный score = актив сильнее рынка
- Отрицательный score = актив слабее рынка

---

#### 3.3.5 VOLATILITY AGENT (Волатильность)
**Вес: 5%**

**Индикаторы:**
- ATR(14) — Average True Range
- Bollinger Band width
- Стандартное отклонение returns

**Методология:**
```python
# Низкая волатильность = положительный сигнал (стабильность)
# Экстремально высокая = предупреждение
# Z-score нормализация
```

**Примечание:** Вес 5% потому что волатильность — это риск, не направление. Используется для фильтрации.

---

#### 3.3.6 INSIDER/FUNDS AGENT (Инсайдеры и фонды)
**Вес: 5%**

**Индикаторы:**
- OBV (On-Balance Volume) — денежный поток
- Volume Surge (аномальные объёмы)
- Институциональное владение (Institutional Ownership %, при доступности)
- Insider trading activity (при доступности)

**Методология:**
- Анализ дивергенций OBV и цены
- Выявление аномальных объёмов (Z-score > 2)

---

#### 3.3.7 MACRO AGENT (Макроэкономика)
**Вес: 5%**

**Индикаторы:**
- VIX (Volatility Index) — страх на рынке
- DXY (Dollar Index) — сила доллара
- 10Y Treasury Yield (^TNX)
- Тренд S&P 500 (рыночный контекст)

**Методология:**
```python
# VIX > 30 = рыночный стресс = понижающий коэффициент
# DXY рост = негатив для emerging markets и крипто
# 10Y yield рост = негатив для growth stocks
```

---

## 4. Техническая архитектура

### 4.1 High-Level Architecture
```
┌─────────────────────────────────────────────────────────────┐
│                      AI ALPHA PULSE                         │
├──────────┬───────────┬──────────┬──────────┬───────────────┤
│  INGEST  │  PROCESS  │  SCORE   │   API    │   FRONTEND    │
│ Yahoo    │ Clean     │ Trend    │ FastAPI  │ React Dash    │
│ Binance  │ Normalize │ Volatil  │ REST     │ Score Table   │
│ MOEX     │ Enrich    │ Sentiment│ WebSocket│ Sparklines    │
│ AlphaV   │ Dedup     │ Fundament│ Auth JWT │ Signal Badges │
└──────────┴───────────┴──────────┴──────────┴───────────────┘
                            │
                    PostgreSQL DB
               (assets, ohlcv, scores)
                            │
                    APScheduler (15min)
```

### 4.2 Технологический стек

| Компонент | Технология | Обоснование |
|-----------|-----------|-------------|
| Backend API | FastAPI | Асинхронность, авто-документация, высокая производительность |
| Database | PostgreSQL | Надёжность, сложные запросы, JSON поддержка |
| Scheduler | APScheduler | Простота, интеграция с Python |
| Frontend | React + Vite | Современный стек, быстрая разработка |
| Charts | Recharts | React-native, кастомизируемость |
| Scraping | yfinance, requests | Бесплатные источники |
| Deployment | Docker + nginx | Портативность, масштабируемость |

### 4.3 Database Schema

```sql
-- Основные активы
CREATE TABLE assets (
    id SERIAL PRIMARY KEY,
    ticker VARCHAR(20) UNIQUE NOT NULL,
    name VARCHAR(100),
    asset_type VARCHAR(20) CHECK (asset_type IN ('stock', 'crypto')),
    exchange VARCHAR(50),
    sector VARCHAR(50),
    created_at TIMESTAMP DEFAULT NOW()
);

-- OHLCV данные
CREATE TABLE ohlcv_data (
    id BIGSERIAL PRIMARY KEY,
    asset_id INTEGER REFERENCES assets(id),
    timestamp TIMESTAMP NOT NULL,
    open DECIMAL(20,8),
    high DECIMAL(20,8),
    low DECIMAL(20,8),
    close DECIMAL(20,8),
    volume DECIMAL(30,8),
    UNIQUE(asset_id, timestamp)
);

CREATE INDEX idx_ohlcv_asset_time ON ohlcv_data(asset_id, timestamp);

-- Результаты скоринга
CREATE TABLE scoring_results (
    id BIGSERIAL PRIMARY KEY,
    asset_id INTEGER REFERENCES assets(id),
    timestamp TIMESTAMP DEFAULT NOW(),
    
    -- Individual factor scores
    trend_score DECIMAL(6,2),
    volatility_score DECIMAL(6,2),
    sentiment_score DECIMAL(6,2),
    fundamental_score DECIMAL(6,2),
    relative_strength_score DECIMAL(6,2),
    insider_funds_score DECIMAL(6,2),
    macro_score DECIMAL(6,2),
    
    -- Final score
    ai_score DECIMAL(6,2) NOT NULL,
    signal VARCHAR(20) CHECK (signal IN ('STRONG BUY', 'BUY', 'NEUTRAL', 'SELL', 'STRONG SELL')),
    explanation TEXT,
    
    -- Metadata
    data_freshness_seconds INTEGER,
    model_version VARCHAR(10),
    
    UNIQUE(asset_id, timestamp)
);

CREATE INDEX idx_scores_asset ON scoring_results(asset_id, timestamp DESC);
CREATE INDEX idx_scores_value ON scoring_results(ai_score);
```

---

## 5. API Specification

### 5.1 Endpoints

#### GET /scores
Возвращает все активы с последними скорами.

**Response:**
```json
{
  "items": [
    {
      "ticker": "AAPL",
      "name": "Apple Inc.",
      "asset_type": "stock",
      "ai_score": 67.5,
      "signal": "BUY",
      "factor_scores": {
        "trend": 78.2,
        "sentiment": 45.1,
        "fundamental": 82.3,
        "relative_strength": 34.5,
        "volatility": 12.0,
        "insider_funds": 15.3,
        "macro": -8.2
      },
      "last_updated": "2026-02-25T20:15:00Z"
    }
  ],
  "count": 6,
  "timestamp": "2026-02-25T20:15:30Z"
}
```

#### GET /score/{ticker}
Детальный скоринг для конкретного актива.

**Query params:**
- `asset_type` — stock | crypto
- `detailed` — true/false (включить объяснения)

**Response:**
```json
{
  "ticker": "AAPL",
  "ai_score": 67.5,
  "signal": "BUY",
  "factor_scores": { ... },
  "explanations": {
    "trend": "Golden Cross MA20/50 + RSI в нейтральной зоне",
    "fundamental": "P/E 28.5 (ниже сектора), ROE 160% (отлично)"
  },
  "raw_data_summary": {
    "price": 189.50,
    "volume_24h": 52000000,
    "market_cap": "2.9T"
  }
}
```

#### GET /history/{ticker}
История AI SCORE для графика.

**Query params:**
- `period` — 1d, 7d, 30d, 90d, 1y
- `granularity` — hourly, daily

#### WebSocket /ws/live
Real-time поток обновлений скоров.

---

## 6. Frontend Specification

### 6.1 Дизайн-система
- **Цвета:** Чёрно-белая база + семантические цвета сигналов
  - STRONG BUY: #22C55E (зелёный)
  - BUY: #86EFAC (светло-зелёный)
  - NEUTRAL: #FBBF24 (жёлтый)
  - SELL: #FCA5A5 (светло-красный)
  - STRONG SELL: #EF4444 (красный)
- **Шрифт:** Space Grotesk (современный, технологичный)
- **Принцип:** Минимализм, данные на первом плане

### 6.2 Экраны

#### 6.2.1 Dashboard (главный)
- Карточки топ-5 BUY и топ-5 SELL
- Таблица всех активов с сортировкой
- Sparkline графики (мини-графики динамики)
- Автообновление каждые 60 секунд

#### 6.2.2 Asset Detail (детальная карточка)
- Заголовок: тикер, название, текущий AI SCORE (большой)
- 7 gauge-баров с визуализацией каждого фактора
- График истории AI SCORE (линейный)
- Таблица сырых метрик
- Объяснения в natural language

#### 6.2.3 Settings
- Настройка весов факторов (для power users)
- API key management
- Уведомления (threshold alerts)

### 6.3 Responsive breakpoints
- Mobile: < 640px (таблица → карточки)
- Tablet: 640-1024px
- Desktop: > 1024px

---

## 7. Источники данных

### 7.1 Бесплатные источники

| Источник | Данные | Лимиты |
|----------|--------|--------|
| Yahoo Finance (yfinance) | OHLCV, фундаментал | ~2000 req/day (unofficial) |
| Binance Public API | Крипто OHLCV, объёмы | 1200 req/min |
| MOEX ISS API | Российские акции | 100 req/sec |
| Alpha Vantage | Новости, OHLCV | 25 req/day (free tier) |

### 7.2 Fallback стратегия
- При недоступности основного источника → переход на mock-данные с предупреждением
- Кэширование последних данных на 24 часа

---

## 8. План развития (Roadmap)

### Phase 1 — Skeleton ✅ DONE
- Project structure, ingestors, basic scorers, FastAPI stub

### Phase 2 — Data + Scoring ✅ DONE
- Все 7 агентов с реальными данными
- Работающий AI SCORE

### Phase 3 — Backend + DB 🔄 IN PROGRESS
- [ ] PostgreSQL schema + миграции
- [ ] Хранение OHLCV + scores
- [ ] APScheduler авто-скоринг каждые 15 мин
- [ ] WebSocket live feed

### Phase 4 — Frontend
- [ ] React dashboard
- [ ] Score table + sparklines
- [ ] Детальная карточка актива
- [ ] График истории AI SCORE

### Phase 5 — Testing + Deploy
- [ ] pytest >80% coverage
- [ ] Docker + docker-compose
- [ ] GitHub Actions CI/CD
- [ ] VPS deploy + nginx + SSL

### Phase 6 — Advanced Features (Future)
- [ ] ML-оптимизация весов под рыночные режимы
- [ ] Бэктестинг стратегий
- [ ] Alerts (Telegram/email при смене сигнала)
- [ ] Portfolio scoring (скоринг портфеля целиком)
- [ ] Social features (публичные портфели)

---

## 9. Монетизация (опционально)

### 9.1 Freemium модель
| Tier | Цена | Возможности |
|------|------|-------------|
| Free | $0 | 5 активов, задержка 15 мин, базовые факторы |
| Pro | $19/мес | Неограниченно, realtime, все факторы, alerts |
| Enterprise | custom | API доступ, white-label, поддержка |

### 9.2 Альтернативы
- Полностью open-source (донаты, спонсорство)
- B2B: API для финтех-стартапов

---

## 10. Требования к запуску MVP

### 10.1 Минимальные требования
- Сервер: 2 vCPU, 4GB RAM, 20GB SSD
- OS: Ubuntu 22.04 LTS
- Python 3.11+
- Node.js 18+
- PostgreSQL 15+

### 10.2 Переменные окружения
```bash
DATABASE_URL=postgresql://user:pass@localhost/aialpha
ALPHA_VANTAGE_API_KEY=xxx  # optional
BINANCE_API_KEY=xxx        # optional (public endpoints work without)
JWT_SECRET=xxx
ENVIRONMENT=production
```

### 10.3 Запуск
```bash
# Backend
cd /workspace/AIAlphaPulse2026
pip install -r requirements.txt
python run.py

# API сервер
uvicorn api.main:app --host 0.0.0.0 --port 8000

# Frontend
cd frontend
npm install
npm run dev
```

---

## 11. Метрики успеха

### 11.1 Технические метрики
- Время ответа API: < 500ms (p95)
- Аптайм: > 99%
- Точность данных: 100% (валидация перед записью)

### 11.2 Продуктовые метрики
- DAU/MAU (после запуска фронтенда)
- Retention Day 7, Day 30
- NPS (опрос пользователей)

### 11.3 Бизнес-метрики
- Конверсия Free → Pro (при монетизации)
- CAC / LTV
- MRR (Monthly Recurring Revenue)

---

## 12. Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Блокировка Yahoo Finance | Средняя | Высокое | Fallback на mock, миграция на официальные API |
| Неточность прогнозов | Высокая | Среднее | Дисклеймер "Не финансовый совет", бэктестинг |
| Конкуренция от TradingView | Высокая | Среднее | Фокус на простоте и AI-интерпретации |
| Нагрузка на сервер | Низкая | Среднее | Кэширование, rate limiting |

---

## 13. Лицензирование

### 13.1 Код
- Core engine: MIT License (открытый код)
- Pro features: Commercial (если применимо)

### 13.3 Дисклеймер
Обязательный текст на всех экранах:
> "AI Alpha Pulse предоставляет аналитические данные исключительно в информационных целях. Это не является инвестиционной рекомендацией. Всегда проводите собственный анализ перед принятием инвестиционных решений."

---

## 14. Приложения

### Appendix A: Список поддерживаемых активов (v0.3)

**Акции США:**
- AAPL, MSFT, GOOGL, AMZN, TSLA, NVDA, META, NFLX, AMD, INT

**Российские акции:**
- SBER, GAZP, LKOH, YNDX, TCSG, MGNT, FIVE, VKCO

**Криптовалюты:**
- BTCUSDT, ETHUSDT, SOLUSDT, BNBUSDT, XRPUSDT, DOGEUSDT

### Appendix B: Методология Z-score
```
Z = (X - μ) / σ

Где:
X = текущее значение индикатора
μ = среднее за период (обычно 60 дней)
σ = стандартное отклонение

Z > +2 → экстремально высокое → score +100
Z < -2 → экстремально низкое → score -100
```

---

**Версия документа:** 1.0  
**Дата:** 2026-02-26  
**Автор:** AI Alpha Pulse Team
